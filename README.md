# DNS Manager (Home Assistant / HACS)

Manage multiple DNS A records across providers (Cloudflare first), supporting automatic public IP
detection and per-record static IP overrides.

## Development

- Integration code: `custom_components/dns_manager/`
- Tests: `tests/`

